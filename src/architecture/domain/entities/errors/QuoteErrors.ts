interface QuoteErrors {
    firstName?: string,
    lastName?: string,
    address?: string,
    city?: string,
    zip?: string,
    email?: string,
    phone?: string,
}

export default QuoteErrors;